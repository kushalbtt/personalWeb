{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start Portfolio Localhost",
            "type": "shell",
            "command": "python3 -m http.server 8000",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "isBackground": true,
            "presentation": {
                "reveal": "always",
                "panel": "dedicated"
            }
        }
    ]
}
